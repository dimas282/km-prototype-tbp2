// Document types based on knowledge asset classification
export const documentTypes = [
  { id: 'metode', name: 'Metode', nameId: 'Metode' },
  { id: 'curriculum', name: 'Curriculum & Training Center', nameId: 'Kurikulum & Training Center' },
  { id: 'innovation', name: 'Innovation', nameId: 'Inovasi' },
  { id: 'final-report', name: 'Final Report', nameId: 'Laporan Akhir' },
  { id: 'manual-book', name: 'Manual Book', nameId: 'Buku Manual' },
  { id: 'specification', name: 'Specification', nameId: 'Spesifikasi' },
  { id: 'lesson-learned', name: 'Lesson Learned / Case Collection', nameId: 'Lesson Learned / Kumpulan Kasus' },
  { id: 'knowledge-sharing', name: 'Knowledge Sharing Material', nameId: 'Materi Knowledge Sharing' },
  { id: 'cop-result', name: 'CoP Result', nameId: 'Hasil CoP' },
  { id: 'knowledge-capture', name: 'Knowledge Capture', nameId: 'Knowledge Capture' },
  { id: 'procedure', name: 'Procedure', nameId: 'Prosedur' },
  { id: 'work-instruction', name: 'Work Instruction', nameId: 'Instruksi Kerja' },
  { id: 'standard', name: 'Standard', nameId: 'Standar' },
  { id: 'regulation', name: 'Regulation', nameId: 'Peraturan Perundangan' },
  { id: 'reference', name: 'Reference', nameId: 'Referensi' },
];

export const departments = [
  {
    id: 'design-engineering',
    name: 'Design & Engineering',
    nameId: 'Desain & Engineering',
    icon: '🏗️',
    owner: 'Engineering Division',
    itemCount: 156,
    topics: documentTypes.map(dt => ({
      id: `de-${dt.id}`,
      name: dt.name,
      nameId: dt.nameId,
      itemCount: Math.floor(Math.random() * 20) + 5,
    })),
  },
  {
    id: 'qhse',
    name: 'Quality & Health Safety Environment',
    nameId: 'Kualitas & Kesehatan Keselamatan Lingkungan',
    icon: '🦺',
    owner: 'QHSE Division',
    itemCount: 45,
    topics: documentTypes.map(dt => ({
      id: `qhse-${dt.id}`,
      name: dt.name,
      nameId: dt.nameId,
      itemCount: Math.floor(Math.random() * 15) + 3,
    })),
  },
  {
    id: 'logistics',
    name: 'Logistik',
    nameId: 'Logistik',
    icon: '🚚',
    owner: 'Logistics Division',
    itemCount: 53,
    topics: documentTypes.map(dt => ({
      id: `log-${dt.id}`,
      name: dt.name,
      nameId: dt.nameId,
      itemCount: Math.floor(Math.random() * 12) + 2,
    })),
  },
  {
    id: 'equipment',
    name: 'Equipment',
    nameId: 'Peralatan',
    icon: '🏭',
    owner: 'Equipment Division',
    itemCount: 38,
    topics: documentTypes.map(dt => ({
      id: `eq-${dt.id}`,
      name: dt.name,
      nameId: dt.nameId,
      itemCount: Math.floor(Math.random() * 10) + 2,
    })),
  },
  {
    id: 'it',
    name: 'Information Technology',
    nameId: 'Teknologi Informasi',
    icon: '💻',
    owner: 'IT Division',
    itemCount: 49,
    topics: documentTypes.map(dt => ({
      id: `it-${dt.id}`,
      name: dt.name,
      nameId: dt.nameId,
      itemCount: Math.floor(Math.random() * 12) + 3,
    })),
  },
  {
    id: 'hcd',
    name: 'Human Capital & Development',
    nameId: 'Human Capital & Development',
    icon: '👥',
    owner: 'HCD Division',
    itemCount: 74,
    topics: documentTypes.map(dt => ({
      id: `hcd-${dt.id}`,
      name: dt.name,
      nameId: dt.nameId,
      itemCount: Math.floor(Math.random() * 18) + 4,
    })),
  },
  {
    id: 'corporate-secretary',
    name: 'Corporate Secretary',
    nameId: 'Sekretaris Perusahaan',
    icon: '📝',
    owner: 'Corporate Secretary Division',
    itemCount: 18,
    topics: documentTypes.map(dt => ({
      id: `cs-${dt.id}`,
      name: dt.name,
      nameId: dt.nameId,
      itemCount: Math.floor(Math.random() * 6) + 1,
    })),
  },
  {
    id: 'risk-integration',
    name: 'Risk & System Integration',
    nameId: 'Risiko & Integrasi Sistem',
    icon: '⚖️',
    owner: 'Risk Management Division',
    itemCount: 28,
    topics: documentTypes.map(dt => ({
      id: `risk-${dt.id}`,
      name: dt.name,
      nameId: dt.nameId,
      itemCount: Math.floor(Math.random() * 8) + 2,
    })),
  },
  {
    id: 'marketing',
    name: 'Marketing',
    nameId: 'Marketing',
    icon: '📈',
    owner: 'Marketing Division',
    itemCount: 22,
    topics: documentTypes.map(dt => ({
      id: `mkt-${dt.id}`,
      name: dt.name,
      nameId: dt.nameId,
      itemCount: Math.floor(Math.random() * 6) + 1,
    })),
  },
  {
    id: 'estimation',
    name: 'Estimation',
    nameId: 'Estimasi',
    icon: '📊',
    owner: 'Estimation Division',
    itemCount: 30,
    topics: documentTypes.map(dt => ({
      id: `est-${dt.id}`,
      name: dt.name,
      nameId: dt.nameId,
      itemCount: Math.floor(Math.random() * 8) + 2,
    })),
  },
  {
    id: 'project',
    name: 'Project',
    nameId: 'Proyek',
    icon: '📋',
    owner: 'Project Division',
    itemCount: 65,
    topics: documentTypes.map(dt => ({
      id: `proj-${dt.id}`,
      name: dt.name,
      nameId: dt.nameId,
      itemCount: Math.floor(Math.random() * 15) + 3,
    })),
  },
  {
    id: 'customer-care',
    name: 'Customer Care',
    nameId: 'Layanan Pelanggan',
    icon: '🤝',
    owner: 'Customer Care Division',
    itemCount: 38,
    topics: documentTypes.map(dt => ({
      id: `cc-${dt.id}`,
      name: dt.name,
      nameId: dt.nameId,
      itemCount: Math.floor(Math.random() * 10) + 2,
    })),
  },
  {
    id: 'legal',
    name: 'Legal & General Affair',
    nameId: 'Legal & Urusan Umum',
    icon: '⚖️',
    owner: 'Legal Division',
    itemCount: 58,
    topics: documentTypes.map(dt => ({
      id: `legal-${dt.id}`,
      name: dt.name,
      nameId: dt.nameId,
      itemCount: Math.floor(Math.random() * 12) + 3,
    })),
  },
  {
    id: 'accounting',
    name: 'Accounting & Cash Operation',
    nameId: 'Akuntansi & Operasi Kas',
    icon: '💰',
    owner: 'Finance Division',
    itemCount: 42,
    topics: documentTypes.map(dt => ({
      id: `acc-${dt.id}`,
      name: dt.name,
      nameId: dt.nameId,
      itemCount: Math.floor(Math.random() * 10) + 2,
    })),
  },
  {
    id: 'project-control',
    name: 'Project Control',
    nameId: 'Pengendalian Proyek',
    icon: '📈',
    owner: 'Project Control Division',
    itemCount: 21,
    topics: documentTypes.map(dt => ({
      id: `pc-${dt.id}`,
      name: dt.name,
      nameId: dt.nameId,
      itemCount: Math.floor(Math.random() * 6) + 1,
    })),
  },
  {
    id: 'property',
    name: 'Property & Building Management',
    nameId: 'Properti & Manajemen Gedung',
    icon: '🏢',
    owner: 'Property Division',
    itemCount: 21,
    topics: documentTypes.map(dt => ({
      id: `prop-${dt.id}`,
      name: dt.name,
      nameId: dt.nameId,
      itemCount: Math.floor(Math.random() * 6) + 1,
    })),
  },
  {
    id: 'digital-transformation',
    name: 'Digital Transformation',
    nameId: 'Transformasi Digital',
    icon: '🚀',
    owner: 'Digital Transformation Office',
    itemCount: 34,
    topics: documentTypes.map(dt => ({
      id: `dt-${dt.id}`,
      name: dt.name,
      nameId: dt.nameId,
      itemCount: Math.floor(Math.random() * 8) + 2,
    })),
  },
];

// Generate sample knowledge assets for each department and document type
export const knowledgeAssets = departments.flatMap(dept => 
  documentTypes.flatMap(docType => {
    const count = Math.floor(Math.random() * 3) + 1;
    return Array.from({ length: count }, (_, i) => ({
      id: `${dept.id}-${docType.id}-${i + 1}`,
      title: `${docType.name} - ${dept.name} Document ${i + 1}`,
      titleId: `${docType.nameId} - ${dept.nameId} Dokumen ${i + 1}`,
      type: (['official', 'validated', 'reference'] as const)[Math.floor(Math.random() * 3)],
      department: dept.id,
      topic: `${dept.id.split('-')[0]}-${docType.id}`,
      documentType: docType.id,
      author: `${dept.owner} Team`,
      date: `2024-${String(Math.floor(Math.random() * 12) + 1).padStart(2, '0')}-${String(Math.floor(Math.random() * 28) + 1).padStart(2, '0')}`,
      fileType: 'pdf' as const,
      tags: [docType.name, dept.name.split(' ')[0]],
      fileName: `${dept.id}-${docType.id}-sample-${i + 1}.pdf`,
    }));
  })
);
